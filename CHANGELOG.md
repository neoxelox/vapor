# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html) with pre-GA policy details defined in `docs/operations/release-process.md`.

## [Unreleased]

### Added

- Real bidirectional sync end to end (Wave 8, C8-1…C8-13). The provider trait (`enumerate`/`stat`/`content_hash`/`begin_upload`/`begin_download`/`delete`/`rename`/`poll_changes` + capability flags + `Transient`/`RateLimited`/`Authentication`/`PreconditionFailed`/`NotFound`/`Permanent` error taxonomy) now fronts a real filesystem reference provider: atomic temp-file+rename writes, strict scope enforcement (symlink-escape / traversal / device-crossing refused), op-id tagging via xattr with an atomic `.vapor-meta.json` side-file fallback (hidden from enumeration), and a bounded in-provider changes feed whose cursor expires into a reconcile + re-baseline. The Phase 2.5 staged-executor simulator is deleted; planner/hash/transfer stages do real work under the existing workgate/throttle caps, transfers are chunked `TransferSession`s that hold at their checkpoint under `Suspended`, and the remote→local pipeline (poll → durable intent → download/apply-delete) advances its cursor only after durable enqueue (at-least-once). Loop prevention ships as the `self_write_cache` pair (local + remote echoes; op-id primary, size-gated content-hash fallback, TTL/entry floors under memory pressure).
- Sync modes (C8-59…C8-66): `syncMode` (`two-way` default, `pull-only`, `push-only`) resolved per profile. One-way modes are strict mirrors of the declared source of truth — divergent edits on the subordinate side are reverted and extra content removed, with cumulative revert/delete counters surfaced through status/diagnostics; they never activate without an explicit per-profile opt-in. Two-way keeps the keep-both guarantee.
- Keep-both conflict machinery (C8-14…C8-18): `{stem}~conflict-{deviceId}-{timestamp}` copies (deterministic, `-{seq}` on collision), a persisted hostname-derived `deviceId`, a durable per-path sync index for divergence detection, restart-safe tombstones with retention pruning, deterministic local/remote race resolution, and a deletion guard — a remote delete never removes local content that has unsynced changes (preservation wins). Durable schema bumps v3→v4 in place (new intent kinds, `sync_index`, `tombstones`); a corrupt DB is quarantined and rebuilt, and the startup reconcile re-derives state.
- Multi-profile runtime (C8-19…C8-26): a `profiles` config array runs each profile as an isolated pipeline with its own SQLite file under `state/profiles/<id>/` (the implicit `default` keeps the legacy path), per-profile provider selection and secret namespacing (`auth.{profile}.{provider}.token`), categorical setting overrides, shared-local-root watch dedup with per-profile fan-out, shared workgate/resource ceilings with MIN-lowering, and blast-radius containment (a panicking profile suspends only itself).
- IPC schema v2 + diagnostics surface (C8-27…C8-31): extended status (scope, provider, per-profile summaries, resource ceilings + idle-boost state, conflict/mirror counters), per-intent "why stuck" diagnostics (`vapor diagnostics`), a bounded in-memory activity timeline (`timelineEventLimit`), and `SetAutoLaunch`/`UpdateExcludes` control methods. All additive; v1 peers still handshake (min supported v1).
- User resource budgets + auto-tuning (C8-32…C8-42): `resourceLimits` hard ceilings (CPU/memory/bandwidth, clamped 1..100) and `idleBoost` dynamic headroom (idle-gated, headroom-gated, slow-up/fast-down ramps, throttle exit snaps to base) enforced through workgate cap scaling, a provider-neutral bandwidth token bucket shared by uploads and downloads, and memory-ceiling cache trimming with documented floors. An auto-tuner nudges the per-tick transfer step on a 90s cadence with rollback-on-regression, bounded by the effective ceilings.
- Google Drive provider (C8-48…C8-54): OAuth 2.0 + PKCE (`vapor auth login google_drive` runs the browser + loopback flow; client credentials via `VAPOR_GDRIVE_CLIENT_ID`/`_SECRET`), tokens only in the platform secret store with proactive refresh + one 401 retry, folder-chain resolve/create for the cloud root, multipart uploads ≤5 MiB and resumable chunked sessions above (256 KiB-aligned auto-sized chunks, `Retry-After`-aware rate-limit classification), `changes.list` polling with 410 → cursor re-baseline, trash-not-purge deletes, op-ids in `appProperties`, and md5-based content hashing (the engine hash stage follows the provider's algorithm). Selectable via `provider = "google_drive"`, gated on the passing provider contract suite; every flow tests offline through a scripted HTTP transport.
- Advanced safeguards (C8-55…C8-58): an active-coding heuristic (rapid code-file churn samples as user-active even without a HID signal), folder priority classes + a bounded `vapor flush` boost window (deferred reconciles release immediately; the remote feed re-polls), a mass-deletion guard (200+ local deletes in 60s pause sync before the storm replicates, with a timeline alert; `vapor resume` re-arms), and `vapor support-bundle` (config + redacted logs + live status/diagnostics/timeline + manifest).
- macOS app config hardening: `VaporConfiguration` gains `deviceId` and preserves unknown `vapor.json` keys verbatim across load/save, so an app-side settings write can no longer destroy runtime-owned configuration (profiles, budgets, provider selection). Swift constants mirror the new config keys, provider names, and sync modes.

- Crash-loop protection now actually fires in production, survives restarts, and is shared across surfaces. `core/lifecycle` persists its bookkeeping (`consecutive_crashes`, `last_crash_at_ms`, pause flag, supervision expectations) to a new durable side-file `<vapor_dir>/state/lifecycle.json` (atomic writes; a corrupt file is a hard error that preserves the file, never a silent reset — resetting could un-pause a crash-looping daemon). New `vapor service check` runs one supervision tick against that state: an unexpected daemon exit is registered with the crash-loop guard and the daemon restarts as soon as policy allows (immediately on the first crash, after 2s/4s/8s backoff, paused after the 5th crash in the 600s window until `vapor service acknowledge`); expected stops are never counted and the same exit is never double-counted. The macOS app drives this via a new 30s `DaemonHealthMonitor` tick — previously nothing detected an unclean daemon exit (`registerUnexpectedDaemonExit` had no production caller) and each surface counted crashes in process memory only. Also new: `vapor service bootstrap` (install + start only when autolaunch is enabled — the app-startup path), `--json` on every `vapor service` subcommand (stable contract locked by tests on both the Rust and Swift sides), `uninstall --keep-running` (skip the explicit stop signal when disabling autolaunch — on macOS launchd still tears the job down when the definition is booted out, matching the app's long-standing toggle behavior; the flag matters on service managers that keep a disabled unit running), and a `vapor service status` report that includes `auto_launch` and the durable crash-loop state, with `CrashLoopPaused` overlaid as a real reportable status (closing the L2-4 spec line). Best-effort `launchctl` invocations also run with captured stdio now, so `vapor service uninstall` no longer leaks `Could not find service …` noise onto the terminal, and failed required invocations include launchctl's stderr in the error. Closes `macos.md` M2-5 / M2-6, `core.md` C4-5.
- The `vapor service` lifecycle is now verified end-to-end on macOS CI against the real launchd (`cli.md` L2-5 / `macos.md` M2-4). A new opt-in `--full` flag on `./scripts/e2e.sh` (used by `test.yml`'s macOS job) appends a service round-trip phase to the Tier E2E suite: install → plist + daemon Running + sandbox IPC reachable → SIGKILL crashes walked through the full supervision schedule (immediate restart, 2s/4s/8s backoff deferrals with no double-counting, durable pause on the 5th crash) → start refused while paused → acknowledge → stop (not counted as a crash) → uninstall. Because that phase installs a real LaunchAgent it is opt-in by design and meant for disposable CI runners: it refuses outright if a `sh.arn.vapor.daemon` LaunchAgent already exists (never clobbers a real install), keeps all runtime state in a repo-local sandbox via `VAPOR_DIR`, and removes the LaunchAgent on exit. The default `./scripts/e2e.sh` run that contributors and agents use locally remains fully host-safe.

### Changed

- The macOS app no longer implements daemon lifecycle logic — it delegates everything to the Rust core through the bundled CLI (`macos.md` M2-1 … M2-4, `core.md` C4-5 + C4-7). The Swift `LaunchAgentController` (plist writing + `launchctl` calls), `CrashLoopGuard`/`CrashLoopPolicy`, and `AutoLaunchSettingStore` family are deleted; the kept `LaunchAgentControlling` seam is now implemented by `VaporCLIServiceController`, which invokes `vapor service … --json` as a subprocess, and `DaemonLifecycleManager` shrinks to a thin facade (operation serialization + login-item registration). One implementation now owns autolaunch persistence, the LaunchAgent definition, and crash-loop policy for every surface, so the app and CLI can no longer drift. `Vapor.app` consequently bundles a third executable: the `vapor` CLI at `Contents/Helpers/vapor` — it cannot live in `Contents/MacOS/` because the default macOS filesystem is case-insensitive and `vapor` would collide with the `Vapor` app binary; the CLI resolves `vapord` from `../MacOS/vapord` when bundled. `apps/macos/scripts/package.sh` builds, copies, signs, and asserts all three executables.

### Fixed

- A reconcile whose comparison walk had already finished could yield "for the next safe slice" instead of completing — forever, when every tick exhausted the slice budget — holding the startup barrier up and starving every intent queued behind it. A finished walk now completes regardless of the slice checkpoint (there is nothing left to slice); a lost reconcile controller aborts defensively instead of wedging the leased intent. Found by the new Tier E2E bidirectional scenario; regression-locked by `requested_reconcile_with_flush_boost_downloads_external_cloud_file_and_drains`.
- An interrupted lease batch can no longer strand intents. `lease_ready_batch` marks every returned row leased up front, but the admission loop could `break` (blocked permit, startup barrier) after processing only part of the batch — the remainder sat invisibly leased until the stale-lease sweep. The unprocessed remainder is now handed back to pending in the same tick.
- `vapor pause` now actually pauses work. The IPC pause request previously flipped the `RunState` snapshot string while the runtime kept releasing, leasing, and executing intents — `vapor status` said `Paused` and nothing changed. The tick loop now gates work admission on the run state: ingest, debounce, and the durable flush keep capturing intent state (so nothing is ever lost while paused) and in-flight executions run to completion, but no new work is released or leased until `vapor resume`. Regression test `paused_daemon_stops_admitting_new_work_and_resume_restores_it` asserts zero leases across paused ticks, continued intent capture, and full backlog admission on resume.
- The Rust and Swift crash-loop guards no longer disagree on the backoff schedule. The Rust port gave the first crash a free restart while the Swift original (and the LaunchAgent policy doc) backed off immediately — so the effective schedule depended on which surface observed the crash, and the parity test file the Rust module cited did not exist. The canonical schedule is now `crash 1 → restart immediately, crash 2 → 2s, crash 3 → 4s, crash 4 → 8s, paused on the 5th` (default policy); the Swift `CrashLoopGuard` is aligned to it, `docs/operations/macos/launchagent-policy.md` documents it, and the new `core/lifecycle/tests/crash_loop_parity.rs` + mirrored Swift test lock both sides to the same contract. The policy doc also stops claiming durable crash counters and an app health tick that were never built — both are marked planned.
- Persisted configuration now actually reaches the daemon. `vapor config set localSyncDirectory …` (and every other key except `autoLaunch`) used to be write-only: the daemon resolved its sync scope and ignore rules from environment variables alone, and `vapor service install` wrote a LaunchAgent with an empty environment. A new tolerant loader in `core/shared::config` reads `vapor.json` at daemon startup with the canonical precedence `VAPOR_*` env var → `vapor.json` → compiled default (`sync_directories::resolve_with_config`, `EventPathFilterOptions::from_environment_and_config`). Both LaunchAgent writers — the macOS app and `vapor service install` — now emit the same policy-shaped plist (environment: `VAPOR_DIR` + `VAPOR_ENV` pass-through only; stdout/stderr under `<vapor_dir>/logs/`), so the two surfaces stop silently clobbering each other's daemon configuration; the app's previous workaround of shoving eight config values through plist environment variables is gone.
- Exactly one daemon per `vapor_dir` is now enforced. A second `vapord` (or `vapor run`) used to race the first on the durable queue and silently steal its IPC socket (`bind_listener` unlinks a pre-existing socket file); `vapor run`'s doc comment claimed WAL-lock detection that never existed. A new `core/daemon::singleton::SingletonLock` takes an OS advisory file lock on `<vapor_dir>/vapord.lock` before anything binds or opens; the second instance exits with `daemon already running`. The lock releases automatically on process exit, including crashes.
- Throttle escalations no longer wait out the hysteresis dwell window. The min-dwell logic held the previous state for *any* transition, so a machine that entered Low Power Mode or critical thermal pressure while dwelling in `Light` kept full-speed caps for up to 5 s. Dwell now applies only to relaxations (toward `IdleDrain`); moves to a more conservative state apply on the next sample. Regression test `escalation_to_more_conservative_state_bypasses_the_dwell_window` covers both directions.
- Events dropped at the bounded fs-ingest boundary are repaired instead of silently lost. The recorder counted overflow drops for diagnostics but nothing consumed the counter, so changes arriving faster than the runtime drained were lost until the next restart. The drain path now schedules a deferred whole-scope reconcile (new `StormReason::IngestOverflow`) covering the unknown lost changes, preserving the "never lose intent state" guarantee under callback-vs-runtime backpressure.
- A checkpoint-paused reconcile no longer multiplies durable intents. Pausing preserved the intent both as a requeued durable row and as a pending scheduler entry, and the next flush inserted a brand-new durable row for the scheduler copy — one duplicate per pause, each later running a redundant whole-subtree reconcile (up to ~4 rows/second during the startup barrier with an active user). The scheduler flush now coalesces per `(path, kind)` against already-pending durable rows inside a single transaction, which also collapses N per-intent fsyncs into one and deduplicates repeated edits to the same path. Regression test `checkpoint_paused_reconcile_does_not_duplicate_durable_rows` holds the queue depth at 1 across repeated pause cycles.
- One inconsistent durable row can no longer kill the daemon. A `complete_leased` / `requeue_leased` that matched zero rows returned `InvalidIntentState`, which propagated out of the tick and made `run_forever` exit the process (and with `KeepAlive=false`, launchd never restarts it). Row-level inconsistencies are now logged and the affected intent dropped (at-least-once semantics make this safe); the loop tolerates up to 5 consecutive tick failures before treating the error as structural and exiting.
- Storm detection can no longer compact the entire sync scope on a busy-but-healthy workload. Per-directory thresholds were counted in every ancestor window up to the watch root, so ~600 events spread across unrelated subdirectories within 2 s tripped the root window and collapsed all pending work into one whole-scope deferred reconcile. The per-directory thresholds now apply strictly below the root; the root compacts only via the dedicated (much higher) global pending threshold. Continued churn is also bounded: a deferred reconcile can be pushed back at most `DEFERRED_RECONCILE_MAX_DELAY_MILLIS` (10 min) past first detection, so a never-quiet subtree still converges.
- Ignore-rule edits now apply without a daemon restart. The compiled `.gitignore` / `.vaporignore` filter was built once at watcher start and never refreshed. The fs-watch callback now flags ignore-file changes on a shared, swappable filter and the runtime thread rebuilds it between ticks (the tree walk never runs in the callback). Paired renames are also actionable now: `RenameMode::From/To/Both` map to delete + create events instead of a single ambiguous `Renamed` record for a path that may no longer exist.
- IPC hygiene fixes: an oversized frame is answered with the contract's `PayloadTooLarge` error (declared size included) before the session closes, instead of a bare disconnect; EOF in the middle of a length prefix or payload is now a protocol error distinguishable from a clean close at a frame boundary; the socket's parent directory is created `0o700` *before* the socket exists, closing the bind-then-chmod permission window; and the daemon bounds IPC service at `MAX_CONCURRENT_CONNECTIONS` (32) with a `CONNECTION_IDLE_TIMEOUT_MILLIS` (5 min) idle reaper so silent local clients cannot park daemon threads forever.
- `VAPOR_DIR=~/path` now resolves against the home directory on the Rust side (matching the Swift app's tilde expansion) instead of producing a literal `./~/path` directory in no-shell contexts like launchd. An explicitly empty `VAPOR_LOCAL_SYNC_DIRECTORY` now falls back to the configured/default value like the cloud variable always did, instead of silently idling the daemon. `vapor-lifecycle`'s `vapor.json` store was rewritten on `serde_json` (same serializer as the CLI): the hand-rolled parser could match `"autoLaunch"` inside string values of unrelated keys and would rewrite a corrupt file; corrupt JSON is now a hard error that preserves the file.

### Changed

- The runtime loop is event-nudged and adaptive instead of a fixed 4 Hz poll. Fs-event callbacks and IPC control requests signal a new `TickWaker`, so reaction to work is immediate; a fully idle daemon (no pending events, no in-flight work) relaxes to a 1 s tick (`IDLE_TICK_MILLIS`), quartering idle wakeups for the invisible-first / battery goal. Durable-queue churn also drops: `synchronous = NORMAL` under WAL (the corruption-safe setting; the queue's at-least-once semantics plus the startup reconcile already cover the lost-transaction window), permit-blocked intents requeue with a 1 s delay (`BLOCKED_INTENT_REQUEUE_DELAY_MILLIS`) instead of every tick, `lease_ready_batch` is a single `UPDATE … RETURNING` statement, and a periodic in-run sweep (`STALE_LEASE_SWEEP_INTERVAL_MILLIS`) recovers leases orphaned mid-run instead of waiting for the next restart.
- `vapord` and `vapor run` now share one composition path: new `core/daemon::bootstrap::run_daemon` owns the startup order (signal handlers → singleton lock → config → state DB → runtime → IPC → tick loop), so `vapor run` gains the IPC endpoint, shutdown handlers, and singleton protection it previously lacked, and the two entry points cannot drift. The runtime is also wired to `core/platform`'s `NativePlatformMetricsSampler` (still static inputs until the per-OS FFI lands) instead of a test sampler, and the duplicated engine-side sampler trait is gone: `core/platform` returns the shared `vapor_shared::ThrottleInputs` (gaining the `thermal_pressure` / `disk_pressure` fields its old snapshot type was missing), and `ThrottleInputs` / `RetryFailureKind` moved to `vapor-shared` so providers classify failures with the engine's retry taxonomy natively (`Provider::ensure_cloud_sync_directory` now returns a typed `ProviderError` and is a required method instead of a silently-succeeding default).
- `vapor auth login` reads the token from stdin by default (`--token -` or omit the flag); passing `--token VALUE` still works but warns that argv leaks into shell history and process listings. `vapor logs --tail N` reads the file backwards in chunks instead of loading the whole (unrotated) log into memory. `vapor service install` warns when it resolves `vapord` from `PATH` instead of a sibling binary. JSON output shapes for `vapor status --json` / `vapor timeline --json` are locked by snapshot-style tests per the §9.2 contract.
- Release/dev tooling: `scripts/hooks.sh` now resolves the hooks directory through git itself, so the pre-commit hook installs correctly in git worktrees (where `.git` is a file, not a directory); the hook keeps the full `clean → lint → test → build` pipeline. `scripts/clean.sh` drops its un-dotted `vapor/logs` entry — a stale typo-duplicate of `.vapor/logs` from the pre-centralization era; no revision ever wrote to that path. `scripts/version.sh` uses `cargo update --workspace` instead of `cargo generate-lockfile`, which silently re-resolved every third-party dependency into the release commit. CI caches SwiftPM at `apps/macos/.build` (the previous `.build` path cached an empty directory) and adds cargo `restore-keys`; the release job states that it produces a *draft* release for manual publishing. The Tier-2 performance gate runs only as part of the release pipeline — `perf.yml` has no standalone or scheduled triggers — and `docs/architecture/testing-strategy.md`, `docs/ci/README.md`, `docs/tasks/core.md` (CT-9…CT-11), and `AGENTS.md §9.5` now say so instead of promising nightly runs. `apps/macos/scripts/package.sh` signs inside-out (daemon first, then the app — `--deep` forced one entitlements file onto every nested binary) and marks the packaged app `LSUIElement` so launch is menubar-first without a Dock-icon flash, per the §2.1 lifecycle model.
- Root `README.md`: the Features section is rebalanced between "available now" and "coming next" (menubar surface, auto-launch + crash-loop protection, live ignore rules, and pause/resume are current; sync-dependent behavior stays on the roadmap; bullets stay surface-neutral), and the Configuration intro notes that Vapor reads `vapor.json` at startup so changes apply on next start (the env var → `vapor.json` → default override order stays documented in `.env.example`, which is developer-facing). The Configuration table intentionally documents the full designed config surface docs-first, including the Wave-8 keys (`syncMode`, `resourceLimits`, `idleBoost`) ahead of their runtime landing. `core/platform` / `core/lifecycle` / `core/cli` are no longer labeled "(planned)". `docs/architecture/ipc-contracts.md` now describes the real wire format (framed JSON envelopes, not JSON-RPC 2.0; no phantom `payload_bytes` field) and the implemented handshake field names; `docs/architecture/data-flow.md` reflects the storm root-exemption, ingest-overflow repair, pause semantics, and the event-nudged loop.

### Changed

- `vapor doctor`'s `vapor_dir` probe now warns when the directory is writable but its mode is more permissive than `PRIVATE_DIRECTORY_MODE` (`0o700`) on Unix. Closes the L1-4 spec line "VAPOR_DIR exists / writable / private" — the previous check only validated the first two. Windows skips the probe (NTFS DACLs scope per-user inheritance and there is no `0o700` analogue). New regression test (`vapor_dir_check_warns_when_permissions_are_loose`) seeds a `0o755` directory and asserts the warning.
- `vapor.json` config keys are now centralized in `core/shared::constants::config::{KEY_*, ALL_KEYS}` (mirrored in the Swift `VaporConstants.ConfigKeys` per AGENTS.md §8.6). The `vapor config get|set` validator pulls from the shared list instead of an inline string array; the macOS Swift app references the same identifiers so the two surfaces cannot drift.
- Drop a few small dead-code paper cuts the audit caught: the `let _ = ConfigCommand::Set { … }` no-op binding in `core/cli/src/main.rs::dispatch`, the unused `ConfigCommand` import that fell out with it, and the stub `log_path_for_diagnostics()` in `core/cli/src/commands/ipc.rs` whose own comment admitted it returned `Path::new("")` and had no callers. Also tighten the misleading hysteresis-cause comment in `core/daemon/src/throttle.rs::evaluate` so it states what the code actually does.

### Added

- Specced the sync-mode (directional sync) feature end-to-end across the planning and architecture docs, ahead of implementation. A new `syncMode` setting (`two-way` default / `pull-only` / `push-only`) selects the sync direction; the one-way modes are strict-mirror (the non-authoritative side is driven to exactly match the source) and resolve per profile with a global default, so one device can keep several read-only `pull-only` mirrors beside a `two-way` folder. One-way modes are opt-in and warn up-front before permanently overwriting/removing content on the subordinate side (no recoverable quarantine); Vapor's never-lose-data guarantee stays scoped to `two-way`. New `docs/architecture/sync-modes.md` design doc (indexed in `docs/architecture/README.md`); `docs/architecture/data-flow.md` gains a directionality section and now scopes keep-both to `two-way`; `docs/plans/core.md §2.5` states intent + priority; `docs/tasks/core.md` C8-59…C8-66 track the work — prioritized within Wave 8, built in the order pull-only → two-way → push-only, and required in the core runtime before the macOS UX in Wave 9 (`docs/tasks/README.md`). Root `README.md` Features + Configuration, `docs/product/status-and-goals.md`, `docs/tasks/macos.md` (M4-2 / M4-5), `docs/tasks/cli.md` (L1-5), and `AGENTS.md §4` are updated to match. No runtime behavior changes yet — implementation lands with the C8 tasks.
- `vapor service` now accepts a `--user` / `--system` flag pair (per `docs/tasks/cli.md` L2-1). `--user` is the default and drives the per-user LaunchAgent / systemd / Task Scheduler entry that already shipped in Wave 6; `--system` is rejected with an actionable error so scripts that want to opt in to the future system-wide install surface fail loudly instead of silently being interpreted as unknown args. The two flags are mutually exclusive at the clap layer. `--system` lands in the optional Waves 12 / 13 once the matching native installers do.
- New Tier E2E verification layer, so autonomous contributors are no longer bounded to in-process unit/integration tests. `./scripts/e2e.sh` runs the real `vapor` + `vapord` binaries black-box through the CLI inside a disposable sandbox under the repo-local `.vapor/e2e/` (never `~/.vapor`, no host service installs, no app launch, no network; `./scripts/clean.sh` removes all residue). The scripted suite covers eight scenarios — config round-trip through `vapor.json`, daemon startup with local-root auto-creation, local writes converging through the durable queue (observed via the `queue_intents` high-water mark and read-only `sqlite3` queries), pause/resume over IPC, the `vapord.lock` singleton refusal, `vapor doctor`, clean-SIGTERM restart recovery on the same state DB, and log hygiene (zero `[ERROR]` lines) — and runs on every PR as the final step of `test.yml`'s macOS job. A `--sandbox` mode provisions the same environment and leaves the daemon running with a printed command cheat-sheet for manual/exploratory feature testing and bug reproduction. The harness surfaced the macOS ~104-byte UDS socket-path cap on its first sandbox run (fixed below — see the deep-`VAPOR_DIR` socket relocation entry under Fixed — and covered by scenario S9). Process doc: `docs/development/e2e-verification.md`; policy: `AGENTS.md §8.4 + §9.5 + §9.8`; taxonomy: `docs/architecture/testing-strategy.md`; agent skills: new `.agents/skills/vapor-e2e` plus a corrected `.agents/skills/vapor-debug` (accurate `VAPOR_DIR` resolution and artifact paths, CLI-first diagnostics, read-only state-DB inspection, framed-JSON-over-UDS instead of the phantom XPC, crash-loop schedule, sandboxed repro flow).

### Fixed

- A deep `VAPOR_DIR` no longer silently costs the daemon its IPC endpoint. `sockaddr_un` caps socket paths at 104 bytes on macOS/BSD (108 on Linux); past it, `bind()` failed and the daemon degraded to running without IPC while `vapor status` claimed it was not running — one WARNING in a log nobody was reading (the new e2e harness caught it on its first sandbox run). Daemon and CLI now share `runtime_paths::ipc_socket_location`: when `<vapor_dir>/vapord.sock` exceeds the shared budget (`constants::ipc::MAX_SOCKET_PATH_BYTES`, 100 bytes) both sides deterministically rendezvous at `<os-temp>/vapor-<fnv1a64(vapor_dir)>/vapord.sock` — same `vapor_dir` → same socket in every process (FNV-1a by hand, since `DefaultHasher` is not stable across separately compiled binaries), distinct `vapor_dir`s cannot collide. The daemon logs the relocation at INFO, refuses a symlinked socket parent (the relocated dir lives under a potentially shared temp root; created `0o700` first), and a new `vapor doctor` probe (`ipc_socket_path`) reports canonical vs relocated placement — Warning when relocated, Failure only if even the relocated path cannot fit. Covered by resolver boundary/determinism unit tests, doctor probe tests, and e2e scenario S9 (deep-`VAPOR_DIR` daemon reachable over the relocated socket, doctor explains it, temp residue cleaned). `docs/architecture/macos/ipc-transport.md` documents the relocation contract.

- `vapor auth login` no longer silently loses every token. `NativeSecretStore` is still backed by the process-local in-memory store on every OS pre-Wave-5 (the Keychain / libsecret / Credential Manager bridges are part of the deferred C4-5 / Wave 12 / Wave 13 work) but the CLI was reporting `auth login: stored token for {provider}` regardless. The token vanished at process exit and the user had no signal that anything was wrong. `core/platform::SecretStore` now exposes an `is_persistent() -> bool` method (default `false`; the trait fakes / Native pre-bridge return `false`, the future Keychain / libsecret / Credential Manager bridges will return `true`); `vapor auth login` emits a stderr warning and tags the success line with `(process-local only)` when the store is non-persistent. `vapor auth status` prepends a matching note in the same case.

- The IPC client now bounds every call with a deadline so a wedged daemon (accepting connections but not answering) cannot hang the CLI. `core/ipc/src/client.rs` exposes `Client::connect_with_timeout` and a `DEFAULT_CALL_TIMEOUT` of 3 s; `Client::connect` applies it to both reads and writes via `set_read_timeout` / `set_write_timeout` after `connect_to_socket`. The CLI maps the resulting `WouldBlock` / `TimedOut` errors onto a new `IpcCliError::DaemonUnresponsive` variant rendered as `vapor: daemon is not responding — check \`vapor logs\``, distinct from the existing `DaemonNotRunning` path which fires when the OS rejects the connect outright. New regression test (`connect_with_timeout_returns_promptly_against_wedged_peer`) binds a UDS, lets the listener accept, holds the stream silent, and asserts `Client::connect_with_timeout` returns inside the L3-7 1 s budget.

- The IPC daemon socket is now created with `0o600` (owner-only) permissions on Unix. `bind_listener` previously left the socket at the umask default, so on a multi-user host any local user could connect to `<vapor_dir>/vapord.sock` and issue control commands (pause / resume / reconcile). `core/ipc/src/transport.rs::unix_impl::bind_listener` now calls `fs::set_permissions` immediately after `UnixListener::bind` using `vapor_shared::constants::runtime::PRIVATE_FILE_MODE`. New regression test asserts the post-bind mode is exactly `0o600`. Also drops the dead `into_inner` method whose comment admitted the implementation was confused.

- IPC transport documentation now agrees with the implementation. `docs/architecture/macos/ipc-transport.md` previously claimed "32-bit big-endian length header" while `core/ipc/src/framing.rs` uses little-endian; the doc now matches the code (and the new Windows / Linux per-OS docs say the same so the codec stays one shared codec across every transport). Adds the missing `docs/architecture/windows/ipc-transport.md` (named pipe at `\\.\pipe\vapord-<user-sid>`, security descriptor, framing, shutdown semantics) and `docs/architecture/linux/ipc-transport.md` (UDS at `<vapor_dir>/vapord.sock`, framing, shutdown), each with the per-OS group `README.md` required by AGENTS.md §10. The transport-agnostic contract in `docs/architecture/ipc-contracts.md` already referenced both files; the dangling links are now resolved.

- `vapor service status` now reports the reverse-DNS service label (`sh.arn.vapor.daemon`) instead of the daemon log filename (`vapord.logs`). The previous report stamp pulled `runtime::DAEMON_LOG_FILE_NAME` for the label field, which was wrong on its face. Centralizes the label in a new `core/shared::constants::service::DAEMON_LABEL` (mirroring `VaporConstants.Daemon.launchAgentLabel` per AGENTS.md §8.6); `core/cli/src/commands/{service,doctor}.rs` consume the constant directly so the LaunchAgent plist filename, the install-time descriptor, the doctor probe, and the status report all agree on one identifier. Regression test in `service.rs` asserts `report.label == DAEMON_LABEL`.
- `vapor status` now reflects live runtime state instead of the boot snapshot. The IPC service's status snapshot was previously written only at startup; the runtime never published updates, so pause / resume / throttle transitions were invisible to every client. `core/daemon::ipc_service` now defines a `StatusPublisher` trait that `DaemonRuntime` calls at the end of every tick (and once on attach), and `core/daemon/src/main.rs` wires the IPC service in via the new `attach_status_publisher` hook. Regression test in `core/daemon/src/runtime.rs` (`attached_status_publisher_receives_fresh_snapshot_each_tick`) attaches a recording publisher, requests a pause through `RuntimeControl`, and asserts the post-tick snapshot reports `Paused`.

### Added

- Wave 7 lands the IPC-driven `vapor` CLI surface and the SecretStore-backed auth commands (`docs/tasks/cli.md` L3-1 … L3-7, L4-1 … L4-3). The IPC `Service` trait grows pause / resume / flush_now / reconcile / timeline methods; a new `core/daemon::runtime_control::RuntimeControl` shared-state struct lets the IPC threads write control requests that the runtime tick observes between iterations (pause/resume flips `RunState`; reconcile enqueues a fresh whole-scope reconcile; flush is informational since the tick already drains opportunistically). New CLI subcommands: `vapor status [--json]`, `vapor pause`, `vapor resume`, `vapor flush-now`, `vapor reconcile`, `vapor timeline [--json]`, `vapor logs [--tail]`, `vapor auth login|logout|status`. The IPC client classifies "daemon not running" via the underlying `io::ErrorKind` so every IPC-backed command exits non-zero within 1 s with `vapor: daemon not running — try \`vapor service start\`` instead of hanging (closes L3-7). The auth surface stores tokens under `auth.<provider>.token` keys via `core/platform::SecretStore`; pre-Wave-8 the supported providers are `filesystem` and `google_drive`, and `login` takes an explicit `--token` argument until the OAuth-PKCE browser flow ships with C8-48.
- Wave 6 phase 2 lands the IPC channel (`docs/tasks/core.md` C5-1 … C5-5). New `core/ipc` crate with the JSON wire format (`Hello` / `HelloAck` / `Request` / `Response` / `ErrorBody` / `StatusResponse` / `IncompatibleVersion`), a length-prefixed frame codec bounded at `vapor_shared::constants::ipc::MAX_PAYLOAD_BYTES` (4 MiB default), a Unix-domain-socket transport on Unix (Windows stubbed until Wave 12), a `Service` trait the daemon implements for dispatch, a `Client` that performs the handshake on connect, and 18 unit + 5 integration tests covering the skew matrix (`N ↔ N`, `N-1 ↔ N`, `N+1 ↔ N`, `|N-M|=2` rejected), payload-bounds rejection, and forward-compat unknown-field tolerance. The daemon's `main.rs` spawns the IPC server at `<vapor_dir>/vapord.sock` on startup; `core/daemon/src/ipc_service.rs` adapts the live `DaemonApp` snapshot into the wire format. New shared constants: `IPC_SCHEMA_VERSION_CURRENT`, `IPC_SCHEMA_VERSION_MIN_SUPPORTED`, `IPC_MAX_PAYLOAD_BYTES`, `IPC_SOCKET_FILE_NAME`.
- Wave 6 phase 1 lands the `vapor` CLI (`docs/tasks/cli.md` L0-1 … L0-5, L1-1 … L1-4, L2-1 … L2-4): a new `core/cli` crate with `clap` dispatch, library-level per-command logic so unit tests drive each command without spawning the binary, and 19 unit tests covering config get/set, doctor checks, and service lifecycle. Implemented commands: `vapor run` (in-process daemon via `DaemonRuntime::start`), `vapor config get|set` (touches one key in `vapor.json` while preserving the rest), `vapor version` and `vapor --version`, `vapor doctor` (macOS-flavor sanity checks: vapor_dir writability, vapord discovery, LaunchAgent plist presence), and `vapor service install|uninstall|start|stop|restart|status` driving `core/lifecycle::DaemonLifecycleManager` over `core/platform::NativeServiceInstaller`. New `scripts/cli/build.sh` and `scripts/cli/test.sh` helpers per L0-3, plus `core/cli/README.md` with the install-from-source flow. The IPC channel + skew-matrix tests (`core.md` C5-1 … C5-5) and the Swift app shim (M2-1..M2-4 / `core.md` C4-5, C4-7) are deferred to Wave 6 phase 2 / Wave 7.
- Wave 5 lands `core/lifecycle`, the cross-surface daemon-lifecycle crate that ports the Swift `DaemonLifecycle.swift` types into Rust verbatim (`docs/tasks/core.md` C4-1, C4-2, C4-3, C4-4, C4-6). `CrashLoopGuard` keeps the same defaults (`baseDelay=2s`, `maxDelay=120s`, `failureWindow=600s`, `maxConsecutiveFailuresBeforePause=5`); `DaemonLifecycleManager` exposes the same public surface (`bootstrap_if_needed`, `set_auto_launch_enabled`, `register_unexpected_daemon_exit`, `start_daemon_if_allowed`, `stop_daemon_for_termination`, `acknowledge_crash_loop_pause`) over a `vapor_platform::ServiceInstaller`; `AutoLaunchSettingStore` reads / writes the `autoLaunch` field in `vapor.json` atomically (temp-file-then-rename) so the Swift `VaporConfigurationStore` and the Rust CLI can share one file. 23 parity unit tests mirror the Swift `DaemonLifecycleManagerTests` scenarios. C4-5 (CLI subprocess bridge) and C4-7 (Swift-side retirement) wait for Wave 6 when the `vapor` CLI ships its `service install / start / stop / status` commands.
- Wave 4 lands `core/platform`, the platform-abstraction layer the engine consumes instead of reaching for OS APIs directly (`docs/tasks/core.md` C3-1 … C3-10). Every trait — `FsWatcher`, `ServiceInstaller`, `SecretStore`, `PlatformMetricsSampler`, `IdleNotifier`, `FilesystemCapabilities`, `ProcessSupervisor` — ships with a Rust trait definition, an `InMemory*` test fake usable on every OS, a macOS native skeleton, and Linux / Windows native stubs that compile but return `Unsupported` so the door stays open for Waves 12 / 13. The macOS `NativeServiceInstaller` writes the policy-approved LaunchAgent plist (Label / ProgramArguments / RunAtLoad=true / KeepAlive=false / ProcessType=Background, plus optional StandardOut/ErrPath and EnvironmentVariables) and drives `launchctl bootstrap` / `kickstart` / `bootout` per `docs/operations/macos/launchagent-policy.md`. The macOS `NativeProcessSupervisor` uses `signal-hook` to translate `SIGTERM` / `SIGINT` into the existing `SHUTDOWN_REQUESTED` flag — closing C3-7 — and `core/daemon/src/main.rs` switches to consuming the trait so its previous inline `libc::signal` handler is gone (closes C1-5: `libc` no longer needs to be a top-level `core/daemon` dependency). New `core/platform/README.md` and an updated `docs/architecture/platform-abstractions.md` describe the module layout, the trait contract per module, and the "adding a new trait / OS" checklist.
- Wave 3 closes runtime gaps `docs/tasks/core.md` C2-3 and C2-4. New `core/daemon/src/clock.rs` defines a `Clock` trait with `now() -> Instant` and `now_system() -> SystemTime`, plus `SystemClock` (production) and `ManualClock` (tests). `DebounceLoop`, `ReconcileController`, `StagedExecutor`, `DaemonRuntime`, and `DaemonApp` all accept the clock at construction; tick cadence (`tick_is_due`), reconcile slice budgets, staged-executor stage timing, and the throttle sample interval now use monotonic `Instant` math, so wall-clock rewinds (DST / NTP / `date -s`) cannot flip elapsed-time gates. `ThrottleController` becomes stateful and enforces hysteresis via `MIN_DWELL_LIGHT_SECONDS = 5`, `MIN_DWELL_THROTTLED_SECONDS = 5`, and `MIN_DWELL_SUSPENDED_SECONDS = 1` (new shared constants); oscillating CPU / network samples can no longer flip the state more than once per dwell window. Boot bypasses the dwell on the very first decision so the engine starts in the right tier immediately. New / refreshed regression tests cover monotonic-axis wall-clock-rewind safety on debounce + reconcile + executor + runtime, and oscillating-CPU stability on throttle.
- Wave 3 first slice closes runtime gaps `docs/tasks/core.md` C2-1 and C2-2. The composed runtime tick path now consumes a real input source via the new `core/daemon/src/metrics.rs` module, which defines a `MetricsSampler` trait with a config-driven `StaticMetricsSampler` (default) and a deterministic `ScriptedMetricsSampler` for tests; `DaemonRuntime::start_with_sampler` injects the sampler so `tick(now)` flows OS-derived inputs (Wave 4 will plug in `PlatformMetricsSampler` behind the same trait). The `ThrottleWorkgate` permit allocator now uses wrapping arithmetic with a collision-skip loop, so the engine survives `u64::MAX` saturation without the previous `saturating_add` bug that re-issued the same permit id forever; two new unit tests walk the counter past the boundary and through alternating acquire/release. Per-stack symlink fixtures in `core/daemon/src/{fs_events,runtime}.rs` are now `#[cfg(unix)]` so the cross-OS CI matrix continues to compile and run the cross-platform suite cleanly on Windows.
- New `.github/workflows/build.yml` runs `./scripts/build.sh` on every pull request and push to `main`, fanning out across `macos-26` (full Rust + Swift), `ubuntu-latest`, and `windows-latest` (Rust workspace only) with `fail-fast: false`. Catches portability regressions on PRs instead of waiting for the tag-driven release flow. `docs/ci/{overview,required-checks,README}.md` document the new workflow alongside `lint.yml` / `test.yml` and list the three new required status checks (`build (macos-26)`, `build (ubuntu-latest)`, `build (windows-latest)`).
- Wave 2 portability foundation lands (`docs/tasks/core.md` C1-1, C1-2, C1-3, C1-4, C1-6, C1-7): the Rust workspace now compiles and runs its full Tier-1 test suite on macOS / Linux / Windows. `core/shared/src/runtime_paths.rs` gates Unix `DirBuilderExt` / `OpenOptionsExt` / `PermissionsExt` behind `#[cfg(unix)]` and provides a Windows fallback that uses inherited NTFS DACLs; cross-platform `home_directory()` honors `HOME` on Unix and prefers `USERPROFILE` on Windows. `core/daemon/src/state_db.rs` ditches the Unix-only `OsStrExt` / `OsStringExt` path-as-bytes encoding for UTF-8 (`path_text TEXT NOT NULL`); pre-GA schema bumps to v3 and rejects v2 outright per AGENTS.md §1.1. `core/daemon/src/fs_events.rs::normalize_absolute_path` now preserves `Component::Prefix` so Windows drive-letter paths survive normalization. The cross-stack orchestrators (`scripts/{lint,format,test,build,locales}.sh`) gate the Swift wrapper invocations behind `uname -s == Darwin`, so the per-stack scripts under `scripts/swift/` stay focused on their work and the cross-OS CI matrix runs `core/*` cleanly. `.github/workflows/{lint,test}.yml` gain a `strategy.matrix` over `macos-26`, `ubuntu-latest`, and `windows-latest` with `fail-fast: false`; `docs/ci/{overview,required-checks}.md` document the new per-OS legs and required-status names. C1-5 (`libc` dep cfg-gate) is intentionally deferred until Wave 4 lands the signal abstraction (C3-7).
- Wave 1 of the cross-surface roadmap (`docs/tasks/core.md` C0-10) is fully closed: residual generic-behavior references to "FSEvents" in `docs/architecture/data-flow.md` (pipeline step, multi-profile watcher dedup, per-profile fan-out callback) are renamed to "fs-watch" so the docs read transport-agnostic, with "FSEvents" reserved for the macOS-specific API. `docs/tasks/README.md` Wave 1 status flipped from "mostly complete" to "complete".
- Locale-sync tooling now mirrors the lint/test/build/format orchestrator pattern: `scripts/swift/sync-locales.sh` is renamed to `scripts/swift/locales.sh`, and a new top-level `scripts/locales.sh` delegates to every per-stack locale script (currently just Swift). The macOS app packaging path, `scripts/swift/{build,test}.sh`, and the cross-stack docs (`README.md`, `docs/development/runbook.md`, `docs/plans/macos.md`) point at the new entry points so future app surfaces can plug into the same orchestrator.
- New `./scripts/hooks.sh` installs a git `pre-commit` hook that runs the local validation pipeline (`clean.sh` → `lint.sh` → `test.sh` → `build.sh`) before every commit, so agentic and human contributors share one bar for "this commit is correct". The installer is idempotent, preserves any pre-existing non-vapor `pre-commit` hook as `.git/hooks/pre-commit.bak`, and `./scripts/hooks.sh uninstall` only removes hooks carrying the `vapor-managed-hook` marker. Documented in `README.md` Scripts list and `docs/development/runbook.md` (entry point list + dedicated "Pre-commit hook" section explaining the trade-off of full re-build per commit vs catching stale-artifact regressions).
- macOS LaunchAgent and crash-loop interaction now have automated coverage of the four policy scenarios from `docs/operations/macos/launchagent-policy.md`: an installed-plist audit asserts the exact policy-approved key set with `KeepAlive=false` / `RunAtLoad=true` / `ProcessType=Background`; a SIGKILL scenario test asserts the lifecycle manager performs no spontaneous restart for 30s after an unclean exit and only resumes on a coordinator-driven trigger; the crash-loop pause now surfaces a reasoned banner and a menubar `Acknowledge and resume` action via a new `AppShellState.crashLoopPaused` field plus `AppShellViewModel.refreshCrashLoopPauseState` / `acknowledgeCrashLoopPause`; and a clean-shutdown coordinator test asserts menubar-quit only stops the daemon and terminates the app (no install/start that would re-engage launchd). Closes the macOS Wave 0 task list (M1-6).
- Documentation is reframed around a portable Rust core with per-platform app surfaces: new plans (`docs/plans/core.md`, `docs/plans/cli.md`), a per-surface task directory (`docs/tasks/core.md`, `docs/tasks/macos.md`, `docs/tasks/cli.md`) with a roadmap-orchestrator `README.md`, a `docs/architecture/platform-abstractions.md` reference for the planned `core/platform` trait catalog, and `README.md` entrypoints in every docs group (`architecture`, `ci`, `development`, `operations`, `performance`, `plans`, `product`, `tasks`) plus per-platform subdirectories (`architecture/macos`, `operations/macos`).
- `docs/plans/original.md` is removed after confirming every section is fully absorbed into the successor plans, task lists, code, and architecture/operations references. Deliberate evolutions from the original intent (cross-platform reframing, `KeepAlive=false` with in-process crash-loop protection, filesystem-first provider phasing, bidirectional in MVP) are each recorded in the successor doc that took over that section.
- Cross-surface roadmap in `docs/tasks/README.md`, the core plan execution sequence in `docs/plans/core.md §10`, the CLI distribution plan in `docs/plans/cli.md §4`, and `AGENTS.md §1` + `§9` + `§11` are reprioritized to make the focus explicit: the primary deliverable is a polished core runtime, `vapor` CLI on macOS, and macOS app. Engine portability fixes and the `core/platform` trait layer (with macOS-native impls + Windows/Linux stubs) stay in the primary path as mandatory foundation. Windows and Linux platform implementations, `apps/windows`, `apps/linux`, and full cross-OS CLI distribution move into a clearly-deferred / optional section (waves 12+) gated on the project owner explicitly opting into a non-macOS surface.
- Testing strategy is codified explicitly to support autonomous-agent development: `docs/architecture/testing-strategy.md` (new authoritative reference) defines the taxonomy — unit / integration / property / platform-trait contract / concurrency / snapshot / guard-rail / fuzz — the "do not test" list (trivial getters, `Default` mirroring constants, UI rendering on every app surface, interactive TTY behavior on the CLI, third-party internals, policy restatements), the per-surface scope (`core/*` heavy, apps logic-only, CLI no TTY), discipline rules (fast, deterministic, independent, scoped, no network, no real `~/.vapor`), a flaky-test policy (no retry decorators), and a two-tier CI model with an explicit 5-minute-per-OS Tier-1 budget. `AGENTS.md §9` is expanded into seven subsections (`9.1`–`9.7`) that match the strategy. New `Phase CT` cross-cutting tasks in `docs/tasks/core.md` adopt `proptest`, add a CI timing guard, audit the existing suite for trivial-test smell, schedule nightly fuzz + higher-case-count property runs, and establish platform-trait contract-test and IPC skew-matrix requirements. `docs/tasks/macos.md` gains `Phase MT` (audit Swift suite, retire duplicates when lifecycle moves to Rust, explicit "no UI tests" rule). `docs/tasks/cli.md` gains `Phase LT` (snapshot every `--json` command via `insta`, explicit "no interactive TTY tests" rule). `docs/plans/{core,macos,cli}.md`, `docs/development/runbook.md`, `docs/ci/README.md`, and the `core/*` + `apps/macos` READMEs are updated to point at the authoritative reference.
- Vapor settings now expose editable baseline and override ignore-rule lists, persisting them for daemon filtering on the next launch.
- Daemon event/intent bookkeeping now stays bounded in memory by compacting over-cap subtrees into a single reconcile intent instead of letting callback path growth run unbounded.
- Daemon debounce/coalescing now runs on a 250ms tick with conservative quiet windows so config edits settle faster while lockfiles and other unmatched paths wait longer before stabilization.
- Daemon scheduling now keeps one latest intent per path, supersedes stale actions, and requeues dirty paths when new changes arrive during in-flight work.
- Daemon throttle control now evaluates 1s power, thermal, load, disk, network, and activity samples to choose `IdleDrain`, `Light`, `Throttled`, or `Suspended` with deterministic worker caps.
- Daemon planner, hash, upload, and reconcile stages now acquire strict throttle-gated work permits so new work cannot exceed the active state's caps.
- Daemon startup now initializes a SQLite durable queue/state DB, recovers leased intents after restart, and persists queue/state metadata with explicit schema versioning.
- Daemon retry scheduling now applies exponential backoff with deterministic jitter, persists the longest rate-limit slowdown window across restarts, and durably finalizes terminal failures.
- Daemon storm detection now converts noisy subtrees into deferred reconcile markers once per-directory or global burst thresholds trip, keeping callback-side path growth bounded earlier.
- Daemon tests now include micro-regression guards for filesystem callback bursts, debounce/coalescing ticks, and scheduler superseding hot paths.
- Daemon reconcile control now starts only in `IdleDrain`, yields on slice expiry or throttle changes, and clears compacted subtree boundaries after successful quiet completion.
- Daemon tests now stress large per-subtree, global-cap, and multi-subtree storm scenarios so bounded memory/backpressure behavior stays covered under heavy pending-intent bursts.
- Daemon startup now composes a real runtime loop that advances watcher ingest, debounce, scheduler draining, durable queueing, throttle-gated work, and idle-biased reconcile progression on each tick.
- Daemon restart recovery now inserts a prioritized whole-scope reconcile and re-prioritizes any existing root reconcile so volatile pre-DB intent loss is reconstructed conservatively before older durable work resumes.
- Daemon callback scope checks now canonicalize the watch root and reject lexically-out-of-scope paths before they can enter bounded ingest state. Per-component symlink resolution runs on the runtime thread (see the Fixed entry below) so the callback stays hot-path safe.

### Changed

- Persisted app config now uses `autoLaunch` and `languageCode`, with English as the default UI language when no other catalog is selected.
- App startup now preserves malformed `vapor.json` files in place, surfaces the load failure in the UI, and avoids silently overwriting broken config with defaults.
- Ignore toggles and saved ignore rules now refresh the in-memory daemon launch configuration immediately so future lifecycle actions stay aligned with persisted settings.
- Placeholder sync-state cycling controls have been removed from the app and menubar until real daemon-backed pause/flush actions exist.
- Runtime path handling now normalizes `VAPOR_DIR`, applies restrictive local permissions to config/log/state artifacts, and redacts sensitive log metadata without panicking on log-file open failure.
- Durable daemon state now redacts and bounds persisted error text, rejects oversized counters or state values, and guards against out-of-range persisted timestamps.
- Pre-GA daemon state now rejects older on-disk schemas instead of carrying forward compatibility shims, and removes an obsolete deferred-reconcile helper API.
- Daemon startup now injects the selected provider through the provider trait boundary instead of hardcoding the Google Drive type inside core daemon orchestration.
- Daemon runtime now advances durable non-reconcile work through bounded planner, hash, and upload stages under throttle/workgate caps instead of processing one leased intent at a time.
- Project framing shifts from "macOS-only background sync product" to "invisible-first background sync product with macOS shipping first and Windows/Linux/CLI following on the same portable Rust runtime". `AGENTS.md` system boundaries, naming conventions (adds the `vapor` CLI), distribution trust chain, toolchain policy, test matrix, definition of done, and docs-update policy are updated accordingly. The root `README.md` install/features/development/agents sections are updated to point at per-group `README.md` entrypoints. `core/*` and `apps/macos` READMEs are rewritten around the portable-runtime vocabulary.
- `docs/architecture/xpc-contracts.md` is renamed to `docs/architecture/ipc-contracts.md` (transport-agnostic). macOS-specific transport details live in `docs/architecture/macos/ipc-transport.md`. Superseded planning files (`vapor-macos-plan.md`, `vapor-macos-task-list.md`, `vapor-macos-distribution-foundation-plan.md`, and the GitHub release-system feature plan) are removed; `vapor-original-plan-verbatim.md` is renamed to `docs/plans/original.md`. `docs/operations/launchagent-policy.md` moves under `macos/`, and `docs/operations/distribution-trust-chain.md` becomes a cross-platform index delegating to the per-OS docs.
- Roadmap now introduces a Phase 3 local filesystem reference provider that exercises every provider-neutral bidirectional mechanic against a loopback backing store, and defers Google Drive integration to Phase 9 so later runtime, safety, profile, XPC, auto-tuning, and provider-extensibility work stabilizes against the reference provider first.
- Roadmap now specifies user-configurable `resourceLimits` (CPU/memory/bandwidth hard ceilings) and `idleBoost` (dynamic headroom expansion when the device is genuinely idle) as a layer over the internal throttle controller, with profile overrides resolving by MIN-lowering, enforcement at the workgate/bandwidth-shaper/memory-compaction layers, and diagnostics surfacing effective ceilings and boost reason codes.
- Design spec now pins deterministic idle-boost / throttle transition behavior (snap-down on `IdleDrain` exit, fresh up-ramp on return, no auto-resume from stale conditions, in-flight work yields at next slice checkpoint), a concrete bidirectional conflict-suffix template (`{stem}~conflict-{deviceId}-{timestampMs}{ext}` with collision-avoidance fallback and "data preservation wins over deletion"), self-write cache constants and xattr/side-file precedence with memory-pressure floors, multi-profile watcher coordination (one watcher per canonical realpath, per-profile queues, shared workgate, panic-contained per-profile runtimes), XPC version-skew rules (`|N - M| <= 1`, typed default-on-unknown-field, payload-size bounds), LaunchAgent plist policy (`KeepAlive=false` with in-process crash-loop backoff) and validation scenarios, SLO applicability under lowered user ceilings, a Phase 2.5 simulator removal checklist blocking Phase 4, a happy-path bidirectional race smoke test in Phase 3, and per-intent "why stuck" diagnostics.
- Documentation pass closes drift introduced during the recent code/spec changes: `data-flow.md` now describes the lexical-only callback plus runtime-thread symlink resolution, `xpc-contracts.md` per-intent stages match `executor.rs::ExecutionStage` plus the `Download`/`WaitingForDownload` Phase 3 stages and the `dropped_incoming_event_count` backpressure metric, `launchagent-policy.md` documents the practical backoff schedule given the 5-crash pause cap and routes validation to P1-6 instead of phases 5/7, `macos-app-lifecycle.md` lists the new provider order and the `CrashLoopPaused`/`acknowledgeCrashLoopPause` flow, `state-schema-migrations.md` reflects the retry-only `attempt_count` semantics and `LEASE_TIMEOUT_MILLIS` recovery, `compatibility-and-upgrades.md` cross-references the XPC version-skew rules, `status-and-goals.md` reflects current pre-GA phase status and the resource-budget / profile / conflict-policy directions, and the planning surface adds explicit `deviceId` schema (P4-1a) and throttle-controller hysteresis (D-3) tasks while clarifying the Phase 5 ↔ Phase 7 ordering for layered resource-budget overrides.

### Fixed

- `scripts/tests/version.sh` no longer crashes when invoked from inside a parent `git` operation (e.g. the project's `pre-commit` hook). The test now `unset`s `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_OBJECT_DIRECTORY`, `GIT_COMMON_DIR`, `GIT_NAMESPACE`, and `GIT_PREFIX` at the top so its `mktemp -d` + `git init` fixture repos resolve cleanly. Without this, git inherited `GIT_DIR` from the hook's environment, "re-init"ed the parent repo instead of the temp dir, and re-fired the pre-commit hook from inside the test fixture — failing every commit that ran under `scripts/hooks.sh`.
- Packaged `Vapor.app` builds now include the SwiftPM localization resource bundle and no longer crash on launch while the app shell resolves UI copy catalogs.
- Packaging now fails fast if either bundled executable is missing, and runtime daemon resolution stays pinned to the bundled `Contents/MacOS/vapord` sibling binary.
- GitHub Actions macOS workflows now run on `macos-26`, matching Vapor's macOS 26-only app target so SwiftUI app tests load against a supported runtime.
- Daemon runtime no longer creates duplicate durable rows each tick when a reconcile fails to start under non-`IdleDrain` throttle; the upserted scheduler intent is now discarded on failed start so the next flush skips it.
- Startup reconstruction barrier now auto-clears after a bounded deadline so non-reconcile work cannot starve indefinitely if the startup reconcile keeps deferring under persistent non-`IdleDrain` pressure.
- Durable intent `attempt_count` now tracks real retry count instead of lease count: `lease_ready_batch` no longer bumps it, `schedule_retry` is the only increment site, and retry caps are enforced at write time so intents cannot reach `MAX_ATTEMPT_COUNT` from throttle-delayed re-leases.
- Durable state `system_time_to_millis` now validates `u128 → i64` conversion symmetrically with the read-path range check, eliminating silent truncation for far-future wall-clock timestamps.
- Durable lease recovery now resets `attempt_count` for leases older than `LEASE_TIMEOUT_MILLIS` (15 minutes) so stale crash-recovered leases don't carry forward inflated retry counts.
- Runtime paths now create private directories and files with restrictive modes at creation time via `DirBuilder::mode` (Rust) and `createDirectory/createFile attributes:` (Swift), including every intermediate directory under the vapor root; the previous two-step create-then-chmod pattern left intermediates at umask defaults and opened a TOCTOU window where the SQLite DB and `vapor.json` were briefly world-readable.
- Log redaction now covers the expanded auth/secret shape set across both daemon and app loggers, matching inline markers for `access_token=`, `refresh_token=`, `api_key=`, `X-Api-Key:`, `client_secret=`, `set-cookie:`, `id_token=`, `session=`, `password=`, and structured metadata keys containing `api_key`, `client_secret`, `refresh`, `oauth`, or `session`.
- FSEvents callback now performs only lexical path normalization and watch-root prefix check; per-component symlink resolution (previously up to 32 `stat`+`readlink` syscalls per event) has moved to the runtime thread, where stabilized events are validated against the real filesystem before entering the scheduler. Events that resolve outside the watch root are dropped with a diagnostic. This restores callback hot-path discipline per AGENTS §3.
- Daemon timing checks no longer stall or silently bypass budgets on wall-clock rewind: the debounce tick now wakes immediately when `duration_since` fails, the reconcile checkpoint treats an impossible-duration as slice-budget-expired, the throttle sampler re-samples on the next tick, and the staged executor advances stages that cannot be measured. Each path has a regression test that walks the clock backwards and asserts the conservative behavior.
- App shell no longer silently rewrites the user-selected `languageCode` to the fallback catalog language on startup. The user's choice is preserved in `vapor.json` across app restarts; the resolved catalog language is surfaced separately as `effectiveLanguageCode` and drives UI strings, so setting an unavailable locale cleanly degrades to English for display without clobbering the persisted preference.
- Swift logger now caches a per-instance `FileHandle`, calls `synchronize()` after every write, and reopens on error instead of opening a fresh handle per log line; heavy-log paths no longer churn thousands of `open()`/`close()` syscalls and crash-time log loss is bounded by the fsync cadence.
- Daemon workgate permit releases no longer silently leak active counts when a stale or mismatched permit is returned: reconcile pause and completion paths now log a diagnostic if `ThrottleWorkgate::release` rejects the permit, surfacing the accounting discrepancy instead of swallowing it.
- Daemon debounce classifier no longer hardcodes `".vaporignore"` and `"vapor.json"`; both file names come from `core/shared::constants` so the classifier can never diverge from the runtime-level names for Vapor's own config and ignore files.
- Daemon now installs a SIGTERM/SIGINT handler that flips a shutdown flag checked by `run_forever`, so `launchctl unload`, `launchctl kill TERM`, or Ctrl-C exit the runtime loop at the next tick boundary instead of terminating mid-lease. Lease recovery on next start still reclaims any in-flight work; the graceful exit just avoids abruptly killing the daemon during an in-progress tick.
- Default provider is now an inert `FilesystemStubProvider` instead of `GoogleDriveProvider`, so pre-GA daemon runs do not point at Google Drive by default. The stub exposes `name = "filesystem_stub"` and reports no remote-changes-feed / no server-side-rename capability so any caller that treats it as real will fail closed. Google Drive remains compiled in and Phase 9 will flip the default once its credentials pipeline is ready.
- App status surfaces the new default provider display name (`Filesystem (stub)`) instead of the hardcoded `Google Drive` string in `AppShellState.initial`; callers that construct state with a real provider name still override it.
- Path-filter startup discovery now skips the heaviest default-ignored directories (`node_modules`, `.git`, `target`, `dist`, `build`, `coverage`, and similar) when walking the watch tree for `.gitignore`/`.vaporignore` files. Those subtrees are still covered by `DEFAULT_PRE_IGNORE_RULES`, so filtering correctness is unchanged, but a fresh start on a tree with millions of `node_modules` files no longer spends minutes enumerating it before the watcher boots.
- LaunchAgent plist writer now emits `StandardOutPath`, `StandardErrorPath`, and `ProcessType = Background` per `docs/operations/launchagent-policy.md`; `AppShellViewModel` wires the stdout/stderr paths under `<vapor_dir>/logs/` so daemon stdout and stderr survive restarts in the expected location.
- `CrashLoopPolicy` defaults and semantics now match the new policy doc: `failureWindow = 600s`, `baseDelay = 2s`, `maxDelay = 120s`, `delayStartsAfterFailures = 1`, plus a new `maxConsecutiveFailuresBeforePause = 5` that drives a durable `CrashLoopPaused` state. `DaemonLifecycleManager` now returns a `CrashLoopDecision` enum (`noDelay` / `backoff(seconds)` / `paused`), exposes `isInCrashLoopPause` and `acknowledgeCrashLoopPause`, and refuses to start the daemon while paused so runaway crashes do not keep restarting in the background.
- `BoundedFsEventRecorder` now records callback events into a small incoming-events queue behind its own mutex and drains the queue into the bounded event/intent maps only on the runtime thread's next `with_state` / `with_mut_state` call. The FSEvents callback no longer contends with the runtime drain's map mutex; the incoming queue is bounded by `MAX_IN_MEMORY_PENDING_PATHS` and drops with a counter rather than growing without limit under a stalled consumer.

## [0.2.0-alpha.3] - 2026-03-10

### Changed

- Simplified GitHub Actions checkout credential handling so release preflight uses the default authenticated checkout session for tag ancestry fetches.
- Added contributor policy requiring concise `CHANGELOG.md` `Unreleased` notes before non-trivial commits.

## [0.2.0-alpha.2] - 2026-03-10

### Changed

- Hardened `lint`, `test`, `perf`, and `release` GitHub Actions workflows with explicit least-privilege permissions.
- GitHub Releases are now documented as the direct installation source for Vapor app builds.

### Fixed

- Release preflight now uses the default authenticated checkout session for its `main` ancestry fetch so tag-triggered releases can complete reliably.
- CI signing and notarization now run behind the protected GitHub `release` environment, verify the imported `Developer ID Application` identity, and pass the temporary keychain into `notarytool` explicitly.
- `./scripts/version.sh` now refreshes `Cargo.lock` through Cargo and keeps workspace package versions aligned with `VERSION` during release preparation.

## [0.2.0-alpha.1] - 2026-03-10

### Added

- Scoped local/cloud sync directory handling, including safe local root creation and strict sync-root boundaries.
- Gitignore-style filtering with recursive `.gitignore` and `.vaporignore` support plus pre/post user ignore rule layers.
- Locale catalogs with deterministic English fallback for app UI text.
- GitHub release automation with lint/test/perf-gated packaging, draft GitHub Releases, checksums, and release runbooks.
- Centralized `VERSION`-driven app + daemon versioning with build commit provenance surfaced in diagnostics and `vapord --version`.

### Changed

- Shared runtime/config constants are centralized across Swift and Rust so script, app, and daemon defaults stay aligned.
- Release preparation now goes through `./scripts/version.sh`, which creates the release commit and matching tag together.

## [0.1.0] - 2026-03-08

### Added

- Initial project foundation for Vapor app and `vapord` daemon.
- Script-first build, lint, test, and packaging workflows.
- Baseline CI workflows for lint and test on `main` and pull requests.

[Unreleased]: https://github.com/neoxelox/vapor/compare/v0.2.0-alpha.3...HEAD
[0.2.0-alpha.3]: https://github.com/neoxelox/vapor/releases/tag/v0.2.0-alpha.3
[0.2.0-alpha.2]: https://github.com/neoxelox/vapor/releases/tag/v0.2.0-alpha.2
[0.2.0-alpha.1]: https://github.com/neoxelox/vapor/releases/tag/v0.2.0-alpha.1
[0.1.0]: https://github.com/neoxelox/vapor/releases/tag/v0.1.0
