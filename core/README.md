# core

Rust workspace crates for the sync engine runtime and shared contracts.

Contents:

- `core/daemon`: `vapor-daemon` crate and `vapord` binary.
- `core/providers`: provider trait/capabilities and cloud provider integrations.
- `core/shared`: shared models/contracts and reusable Rust utilities (including logging).
